[
  {
    "name": "/start sends the empty-state dashboard for a new user",
    "steps": [
      {
        "send": { "text": "/start", "userId": 1, "chatId": 100 },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "📊 *Your habits*\n\n🌱 _No habits yet — tap ➕ below to add your first one._"
            }
          }
        ]
      }
    ]
  }
]
